{
  "$schema": "https://raw.githubusercontent.com/devcontainers/spec/main/schemas/devcontainer.schema.json",
  "name": "Aspire with Dapr (Local)",
  "extends": "./devcontainer.json",
  
  "runArgs": [
    "-v", "/var/run/docker.sock:/var/run/docker.sock",
    "--network=host"
  ]
}
