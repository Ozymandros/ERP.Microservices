apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../dev
  - letsencrypt-clusterissuers.yaml

patches:
  - path: patch-aspnetcore-environment.yaml
  - path: patch-gateway-ingress-letsencrypt.yaml
