apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mssql-data
  namespace: myapp-apps
spec:
  storageClassName: gp3
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi
