{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning",
      "Ocelot": "Information"
    }
  },
  "AllowedHosts": "*",
  "ALLOWED_ORIGINS": "http://localhost:3000;http://localhost:5000",
  "Jwt": {
    "Issuer": "http://localhost:6001",
    "Audience": "erp-api",
    "RequireHttpsMetadata": false
  },
  "Gateway": {
    "PublicBaseUrl": ""
  },
  "GatewayConfigurationNotes": "Set Jwt__SecretKey via environment variable or User Secrets (never commit secrets). Ocelot public BaseUrl: set Ocelot__GlobalConfiguration__BaseUrl to override ocelot.json. Optional Gateway:PublicBaseUrl for Scalar when it must differ from the request URL after UseForwardedHeaders."
}
